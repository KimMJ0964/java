package test.t240726.object1;

import java.util.Comparator;

public class AscTitle implements Comparator<Music>{
	
	@Override
	public int compare(Object o1, Object o2) {
		o1.getTitle().comepareTO(o2.getTitle());
	}
}
